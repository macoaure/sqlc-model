package config

// SupportedVersion is the only configuration schema version this generator
// accepts (FR-008). There is exactly one supported version for this initial
// baseline; see contracts/config.schema.json.
const SupportedVersion = 1
