// Package config decodes and validates the plugin's `options` block: schema
// version gating, bounded-context/model/field structural validation, and
// order-preserving decode of the declaration-ordered `models` and `fields`
// objects.
package config
